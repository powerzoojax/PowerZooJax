# Spaces

!!! note "Python API 签名"
    本页只翻译概览、合约表与示例。完整的 mkdocstrings 自动生成签名（参数、字段、类型）由英文 API 渲染，见 [English API → Spaces](../../en/api/spaces.md)。

PowerZooJax 用轻量、JAX 友好的 space 描述符表达观测与动作。

## Space 类型

`Space`、`Box`、`Discrete`、`MultiDiscrete` 与 `MultiBinary` 的完整签名由英文 API 页渲染。

## 辅助构造器

`make_box`、`make_discrete`、`make_multi_discrete` 与 `make_multi_binary` 的完整签名由英文 API 页渲染。

## Environment 合约

所有环境都实现同一套高层接口：
