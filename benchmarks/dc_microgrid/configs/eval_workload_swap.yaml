split: workload_swap
ood_scenario: workload_swap
n_eval_episodes: 10
notes: 'OOD: cpu_profile source switched from Google to Azure (distribution shift
  in workload patterns). Requires real Azure workload data; benchmarks/dc_microgrid/{train,eval,baselines,dump_trajectory}.py
  now raise instead of falling back to synthetic if the parquet is missing.'
