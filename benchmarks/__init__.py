"""PowerZooJax benchmark experiment surface.

This is the experiment-running entry point — distinct from the
``powerzoojax`` library package.  It is a regular package (not a namespace
package) so that it cannot be silently shadowed by a sibling repo's
package of the same name (e.g. ``PowerZoo/benchmarks/``).
"""
